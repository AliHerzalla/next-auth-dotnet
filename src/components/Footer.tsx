import React from "react";

const Footer = () => {
  return (
    <div
      className={
        "h-[50px] flex items-center justify-center fixed bottom-0 left-0 w-full text-center"
      }
    >
      Footer
    </div>
  );
};

export default Footer;
